# Verification Status — Thorne Taxonomy

**Last updated:** July 5, 2026 (Etapa 1)

## The three boxes

Following Kip Thorne's discipline for *Interstellar* — every scientific element
labelled as **established truth**, **educated guess**, or **speculation**, and
never allowed to masquerade as one another — every statement in this repository
falls into exactly one box:

### 📗 Box 1 — Established truth (machine-checked)
Statements proven in Lean 4 with `#print axioms` showing only the standard
foundations (`propext`, `Classical.choice`, `Quot.sound`) — or additionally
depending *only on explicitly named hypotheses in their own signature*.
After the Etapa 0/1 cleanup, the Phase 2 theorems are honest **conditionals**:
"IF the named physical assumptions hold, THEN…". The mathematical content of
most is elementary (transitivity, monotone-function bounds); their value is
organizational, not mathematical depth.

### 📙 Box 2 — Educated guess (literature-backed assumptions)
`axiom` declarations matching results known in mathematical physics but never
formalized in Lean (Uhlenbeck compactness, Bishop-Gromov, cluster expansion
bounds, Atiyah-Singer...). Marked 🔬 in AXIOM_AUDIT.md. Each could in principle
be replaced by a real proof — at a cost of person-years each.

### 📕 Box 3 — Speculation (open problems and heuristics)
`axiom` declarations equivalent to open problems — including the existence of
the Yang-Mills measure, the mass gap itself (`mass_gap`, `Delta0`,
`mass_gap_tendsto_continuum`), gap persistence, Gribov cancellation, and all
holographic principles. Marked 🔴 in AXIOM_AUDIT.md. **No theorem that
consumes a Box-3 axiom constitutes progress on the Clay problem.**

### 🗑️ Removed — formerly a fourth, illegitimate box
`gemini_*` axioms recorded LLM assertions ("confidence 1000%") as mathematics.
As of Etapa 0/1 they are deleted (21 orphans) or converted into explicit
hypotheses (`def ...Assumption : Prop`) that theorems must declare openly.
Phase 1 still contains 61 pending conversion (see PHASE1_GEMINI_CONVERSION_MAP.md).

## Current state (branch etapa0-higiene)

| Item | Status |
|---|---|
| CI | 3 jobs green on `main` (Phases 1, 2, 3) |
| Phase 2 | 25/25 modules compile; theorems conditional with named hypotheses |
| Phase 1 | Restructured; 10/76 modules compile; rest catalogued (PHASE1_BUILD_STATUS) |
| **Phase 3 (LatticeGauge)** | **18 files, ~83 theorems, 0 axioms — pure Box 1:** Wilson action, gauge invariance, Gibbs measure (0 < Z ≤ 1), Wilson loops (closed-loop gauge invariance), expectation values (⟨c⟩ = c, |⟨f⟩| ≤ C), the capstone |⟨Wilson loop⟩| ≤ 1 with proved uniform action bound, and GAUGE INVARIANCE OF THE GIBBS EXPECTATION (⟨f∘gauge⟩ = ⟨f⟩ for bi-invariant measures, via measure-preserving gauge action), and the PHYSICAL character on U(n): normalized real trace with χ(1)=1, class function, |χ| ≤ 1 (unitary column bound) — the abstract framework instantiates on genuine matrix gauge groups; HAAR PROBABILITY MEASURE ON U(n) constructed from first principles (topological group instances, compactness via Tychonoff, RIGHT invariance proved via Haar uniqueness) with UNCONDITIONAL capstones: |⟨W⟩| ≤ 1 and gauge invariance of the Gibbs expectation on U(n); TRANSLATION INVARIANCE of action, measure and Gibbs state; truncated correlations with a-priori bound; and — stated, NOT proved — the formal definition HasLatticeMassGap (open target, stated not proved); and the ELEVENTH STONE: at β = 0 the Gibbs state is the product Haar state, observables with disjoint link-supports FACTORIZE (independence), and truncated correlations VANISH — the first proven clustering statement (trivial regime, honestly labelled), the base camp of the cluster expansion; and the TWELFTH STONE (inter-lab collaboration: strategy by GPT-5.6 "Sol"/OpenAI, execution by Claude Fable 5/Anthropic, verdict by CI): the SINGLE-LINK LAW — one link variable is exactly Haar-distributed (eval pushforward of the product measure), linkCharacterIntegral defined, ⟨ψ(U_ℓ)⟩₀ = ∫ψ dμ; and the THIRTEENTH STONE: the joint law of two distinct links is μ⊗μ (pair independence), two-point factorization, connected single-link correlator = 0 at β=0; and the FOURTEENTH STONE (architecture: Sol/GPT-5.6 incl. the epistemic veto of a false Wilson-loop product formula; implementation: Fable via Measure.pi_eq boxes): n-LINK INDEPENDENCE — any finite injective family of links has joint law μ^⊗ι, with n-point factorization and ⟨∏fᵢ(U ℓᵢ)⟩₀ = ∏ linkCharacterIntegral; and the FIFTEENTH STONE — THE FRESH-LINK THEOREM (architecture Sol/GPT-5.6, execution Fable): holonomies of nonempty paths whose first link does not reappear (in particular Nodup paths) are HAAR-DISTRIBUTED, via Haar absorption (X·Y and X⁻¹·Y remain Haar for independent Y) with no probabilistic induction; and the SIXTEENTH STONE — the first CLOSED FORMULA for a physical observable: ⟨Wilson loop⟩₀ = ∫ χ dμ for every nonempty non-self-repeating loop (the β = 0 sector of this finite-lattice model characterized exactly); Haar on U(n) is also INV-INVARIANT (uniqueness mirror), making the fresh-link theorem and ⟨Wilson loop⟩₀ = ∫χ dHaar unconditional on the physical gauge group (with a presentation corollary for closed loops); and the SEVENTEENTH STONE — the first β > 0 result: the FINITE-VOLUME CONTINUITY BOUND from β = 0, |⟨f⟩_β − ⟨f⟩₀| ≤ 2CBβ·exp(βB), with window and Wilson-path corollaries (finite-volume only; B grows with the lattice); and the EIGHTEENTH STONE — LINEAR RESPONSE: d/dβ⟨f⟩_β|₀ = −Cov₀(f, S), the first explicit bridge between the β-perturbation and product-state correlations (finite volume, derivative at zero, no thermodynamic uniformity); and the NINETEENTH STONE — the FIRST-ORDER TAYLOR REMAINDER: |⟨f⟩_β − ⟨f⟩₀ + β·Cov₀(f,S)| ≤ 4CB²β²·exp(βB) for βB ≤ 1, closing the β=0 analysis package. TWENTIETH STONE — the FLUCTUATION–RESPONSE IDENTITY in finite volume: d/dβ⟨f⟩_β = −Cov_β(f, S) at every β₀ ≥ 0, with gibbsCovariance as a first-class definition — the response of any bounded observable to the coupling is exactly its correlation with the action, at any temperature. TWENTY-FIRST STONE — the SECOND-RESPONSE / THIRD-CUMULANT identity: d/dβ[−Cov_β(f,S)] = Cov_β(f,S²) − 2⟨S⟩_β·Cov_β(f,S) at every β₀ ≥ 0, built entirely on top of stone 20 (no iteratedDeriv wrapper claimed). TWENTY-SECOND STONE — FIRST- AND SECOND-RESPONSE IDENTITIES FOR WILSON OBSERVABLES: the pair (20,21) specialized to W = χ(holonomy), with the action bound produced internally; unconditional U(n)/Haar corollaries where only structural conditions (NeZero N, NeZero n, β₀ ≥ 0, closed path) remain. TWENTY-THIRD STONE — the LOG-PARTITION RESPONSE identity: d/dβ log Z_β = −⟨S⟩_β at every β₀ ≥ 0 with pointwise sign ≤ 0 (log partition function, deliberately not called free energy). TWENTY-FOURTH STONE — GIBBS VARIANCE POSITIVITY AND ACTION-FLUCTUATION RESPONSE: Var_β(f) ≥ 0 by the centered-square route, and d/dβ[−⟨S⟩_β] = Var_β(S) ≥ 0 — the first genuine fluctuation inequality (no formal convexity wrapper claimed). TWENTY-FIFTH STONE — EXACT FACTORIZATION OF LINK-DISJOINT WILSON OBSERVABLES AT β=0: pathLinkSet geometric API + stone-11 factorization; ⟨W₁W₂⟩₀ = ⟨W₁⟩₀⟨W₂⟩₀ and vanishing truncated correlation, unconditional on U(n). TWENTY-SEVENTH STONE — ONE-VS-BLOCK VANISHING OF THE THIRD CONNECTED CORRELATION AT β=0: κ₃(f₁,f₂,f₃) = 0 when supp f₁ is disjoint from the UNION of the other two — f₂,f₃ may remain correlated; pairwise disjointness is a corollary; order 3 only, no higher-cumulant claim. TWENTY-EIGHTH STONE — MEASURE-LEVEL INDEPENDENCE AND JOINT LAW AT β=0: link-disjoint observables are IndepFun in the official Mathlib sense and the joint pushforward law equals the product of the marginals — strictly stronger than moment factorization; stone 11 recovered as a standard corollary. TWENTY-NINTH STONE — MUTUAL INDEPENDENCE of link-disjoint families at β=0 (official iIndepFun): every finite subfamily satisfies the intersection-product law; block-vs-block independence for free; arbitrary index type. THIRTIETH STONE — FINITE JOINT TUPLE PUSHFORWARD LAW: the random vector of a mutually independent family equals the product distribution of its marginals (generic probabilistic theorem mirroring measurePreserving_pi; unconditional for closed link-disjoint Wilson loops on U(n)). THIRTY-FIRST STONE — COORDINATE-WISE POST-COMPOSITION STABILITY: every family of derived measurable statistics of link-disjoint Wilson loops inherits the product law at β=0 (simple types only; no dependent codomains or block subtypes). THIRTY-SECOND STONE — FINITE PLAQUETTE-ACTIVITY EXPANSION, level (a) only: exact local decomposition of the Wilson action; gibbsWeight = ∏(1+m_p) = Σ over SUBSETS of activity products; local bound |m_p| ≤ 2β; realZ as a finite sum of activity integrals (signed activities ⇒ realZ, not the ℝ≥0∞ partition function). NOT a connected-cluster expansion; no convergence, no volume uniformity, no clustering, no mass gap. THIRTY-THIRD STONE — FINITE PLAQUETTE CONNECTIVITY: link-sharing adjacency graph, connectivity within a Finset (SimpleGraph.induce encapsulated), full component algebra, and the geometric capstone that DIFFERENT COMPONENTS HAVE DISJOINT LINK SUPPORTS — the formal bridge to stone 26, not yet applied (pure finite combinatorics; no polymer objects, no factorization, no convergence). THIRTY-FOURTH STONE — FINITE CONNECTED-COMPONENT FACTORIZATION OF THE MAYER TERMS: each Mayer term factorizes over the connected components of its subset, and realZ_β = Σ_{A⊆P} ∏_{C∈Comp(A)} E₀[block C] — an exact finite-volume identity; the sum is still over all subsets (no polymer reindexing, no convergence, no log Z, no mass gap). THIRTY-FIFTH STONE — PLAQUETTE POLYMERS: a polymer is a nonempty, admissible, intrinsically link-connected plaquette set; NEW path lemma (walks stay in components); componentFamily A is a compatible polymer family with union A; polymer weights defined. Inverse correspondence and polymer-gas reindexing deliberately deferred (stone 36); no convergence. THIRTY-SIXTH STONE — EXACT FINITE POLYMER-GAS REPRESENTATION: path confinement (walks cannot cross between compatible polymers), each polymer is exactly a component of the union, bijection between admissible subsets and compatible families, and realZ_β = Σ_{Γ compatible} ∏_{C∈Γ} w_β(C) — a finite hard-core gas; the exact part of the expansion (Mayer → components → polymers → gas) is COMPLETE; no convergence, no log Z, no mass gap. THIRTY-SEVENTH STONE — FINITE URSELL COEFFICIENTS: φ(γ) ∈ ℤ as a finite signed sum over connected spanning edge sets of the incompatibility graph (index graph with i ≠ j; canonical internal edges); unit case 1, binary case 0/−1, disconnected ⟹ 0 proved structurally; the (1−1)^m cancellation kept as a separate tool. DELIBERATELY NOT log Z: even in finite volume the cluster series is infinite; permutation symmetry deferred to stone 38. THIRTY-EIGHTH STONE — PERMUTATION INVARIANCE: ursellCoeff (γ∘σ) = ursellCoeff γ via graphUrsellCoeff (G.comap σ) = graphUrsellCoeff G (explicit sum_bij on canonical edge sets; connectivity through a SimpleGraph.Iso; all order cases in one canonicalization helper) — the coefficient depends only on the unordered tuple of occurrences, including multiplicities, as stone 37 promised. THIRTY-NINTH STONE — ELEMENTARY BOUNDS AND TREES: |φ(G)| ≤ 2^|E| (explicitly insufficient for convergence — order-n² exponential) and the structural base case: a tree has exactly one connected spanning edge set, so φ(T) = (−1)^|E(T)|, |φ(T)| = 1 (via abstract edge-essentiality and Mathlib bridges). FORTIETH STONE (part a) — CANONICAL BFS TREE EXTRACTION on Fin (n+1), root 0: total deterministic construction (least-labelled parent one generation below); under connectivity: subgraph, exactly n edges, connected, and root distances formally EQUAL to the original (not just named BFS). IsTree/retraction recorded as an explicit stop (40b) pending the architect ruling — no cardinal converse in the pinned Mathlib — CLOSED by part (b): a LOCAL cardinal converse (connected + n edges on n+1 vertices ⟹ tree) proved by edge minimality with no cycle surgery; penroseTree is a tree, trees are fixed points of the extraction, and the extraction is idempotent — items 9 and 11 of the stone-40 spec fully discharged. FORTY-FIRST STONE (part a) — THE PENROSE CLOSURE: R_G(T) with same-generation (P1) and strict-above-parent consecutive-generation (P2) edges; T ≤ R_G(T) ≤ G; HEART A proved — every edge of a fibre is tree/P1/P2 by parent minimality, so π(H) = T ⟹ T ≤ H ≤ R_G(T). Converse direction and distance preservation of the closure deferred to 41b — DONE (part b): closure edges are 1-Lipschitz in depth, every graph in [T, R_G(T)] has exactly the BFS levels of T, the minimal parent is preserved (extra P2 candidates allowed, strictly above), and THE FIBRES OF THE CANONICAL EXTRACTION ARE EXACTLY THE PENROSE INTERVALS on connected subgraphs, with T the minimum and R_G(T) the maximum (link-disjoint, not distance decay; no claim for β>0). TWENTY-SIXTH STONE — EXACT FINITE-FAMILY FACTORIZATION AT β=0: a GENERIC theorem (any finite family of measurable observables with pairwise link-disjoint supports factorizes under the product measure), with Wilson loops as a geometric corollary — mutual independence in product-expectation form, unconditional on U(n) (value, continuity, derivative, remainder) by elementary means. FORTY-SECOND STONE (part a) — EDGE-SET PARTITION AND FREE FIBRE COORDINATES: the stone-41 fibre theory transported to Finsets of ordered edges via the graphOfEdges/availableEdges inverse bridges; spanningTreeEdgeSets; the fibre of a spanning tree ET is EXACTLY the boolean interval [ET, R_G(ET)] in edge sets; the extra edges form a free switch panel (explicit inverse maps E↦E\ET, F↦ET∪F, asserted ONLY under the spanning-tree hypothesis), with cardinality |ET∪F| = |ET|+|F| and sign factorization; fibres are pairwise disjoint and their union over spanning trees is exactly the connected spanning edge sets. NO sums reindexed, no (1−1)^m applied, no Penrose identity yet (42b) — DONE (part b), THE PENROSE CANCELLATION: two formal reindexations (Ursell sum grouped by the disjoint fibres; each fibre traded for the powerset of its extra edges), the stone-37 (1−1)^m lemma finally consumed (fibres with extra edges cancel IN PAIRS, contributing exactly zero), surviving PENROSE TREES (spanning trees equal to their own closure), every spanning tree on Fin (n+1) has exactly n edges (stone-40 retraction), and the EXACT IDENTITY graphUrsellCoeff G = (−1)^n · #PenroseTrees(G) with |φ(G)| = #PenroseTrees(G) ≤ #SpanningTrees(G) — the finite TREE-GRAPH BOUND, replacing the 2^C(n+1,2) scale of stone 39 by the spanning-tree scale; sanity checks: disconnected graphs (both sides vanish, consistent with the structural stone-37 zero) and trees (recovering φ(T) = (−1)^n, |φ(T)| = 1); Fin 0 separate and trivial. NO tree counting (no Cayley/Prüfer), no polymer activities in the estimate, no 1/n!, no series, no convergence criterion, no clustering, no mass gap. FORTY-THIRD STONE — THE HARD-CORE TREE-GRAPH BOUND FOR POLYMER TUPLES (with the architect ruling that the tree STRUCTURE, not the raw tree count, is the interface that survives toward a Kotecký–Preiss-type criterion): the stone-42 graphic bound specialized to the incompatibility graph of a tuple; the spanning-tree count presented as a sum over labelled trees of the COMPLETE graph with a hard-core indicator product on the edges (each edge remembers exactly which two polymer occurrences are incompatible; trees incompatible with the tuple weigh zero; the labelled tree universe is independent of the tuple); capstone |ursellCoeff γ| ≤ Σ_{trees of ⊤} Π_{edges} hardCore, in natAbs and with real coercions; sanity: pairwise-compatible tuples kill every tree weight, tree-shaped incompatibility gives exactly 1, the single occurrence is the empty-product 1, Fin 0 separate. NO activities, no 1/(n+1)!, no series, no convergence; the parent-function count (n+1)^n DEFERRED and registered. FORTY-FOURTH STONE — THE EXPONENTIAL ACTIVITY BOUND: |polymerWeight_β(C)| ≤ (2β)^|C| for β ≥ 0 and ANY finite plaquette block (stone-32 local bound pushed through the product with NO independence inside the block — plaquettes may share links; triangle inequality for the Bochner integral, pointwise monotonicity, integral of a constant in the β=0 probability state with no residual μ(univ) factor); polymer specialization as documented corollary (predicate as hypothesis, never in the weight type); the empty block saturates with EQUALITY; at β = 0 every nonempty block has weight exactly zero; for β ≤ 1/2 all weights are ≤ 1 (NOT a convergence statement). Volume-independent in the precise local sense only: no distance/diameter decay, no sums over polymers, no cluster series, no Kotecký–Preiss, no clustering, no mass gap. FORTY-FIFTH STONE (part a) — COVERING THE INCOMPATIBILITIES BY LINKS: to control the whole incompatible neighbourhood of a finite block C it suffices to stand guard at each of its at most 4·|C| support links — for any nonnegative weight a on the finite polymer universe, Σ_{D incompatible with C} a(D) ≤ Σ_{ℓ ∈ support(C)} Σ_{D ∋ ℓ} a(D) ≤ 4·|C|·B whenever every rooted per-link sum is ≤ B (overcounting of multi-link sharers deliberately accepted via nonnegativity; one new abstract Finset lemma, sum over an overlapping biUnion ≤ sum of sums, by local induction with sum_union_inter); |plaqLinkSet| ≤ 4 claimed only as an inequality (dimension-free, robust to periodic identifications); the KP-shaped weight |w_β(D)|·exp(α·|D|) specialized and bounded pointwise by stone 44. NO counting of polymers using a link, no M(d)/Δ(d), no Euler tours, no size sums, no α/β choices, no Kotecký–Preiss, no series, no convergence, no thermodynamic limit (stone 45b registered as the geometric bottleneck) — part b-i DONE, THE UNIFORM LOCAL GEOMETRY: with the real representation censused (Site N = (Fin N)^4, Dir = Fin 4 — dimension FIXED at 4, no d parameter; no candidate formula assumed), the volume-free constants are literally typed with no N: at most 16 = card(Fin 4 × Dir) plaquettes use a fixed link (four side classes, each injecting into Dir via shift-injectivity in the Fin N group), the stone-33 plaquette adjacency graph (the one defining connected polymers) has official SimpleGraph degree ≤ 64, every polymer using a link contains an admissible root plaquette using it, and the rooted-link sums reduce to rooted-plaquette sums: Σ_{D incompatible with C} kpWeight(D) ≤ 4·|C|·(16·B) whenever every rooted-plaquette sum is ≤ B. The ONLY remaining gap toward a small-β criterion is the size-counting of connected polymers containing a fixed plaquette (45b-ii: Euler walk vs Dyck code routes, both mapped, neither implemented) |
| gemini_* axioms | 0 anywhere |
| Claim on Clay problem | **None.** See README. |

## Rule going forward

New material enters `main` only if: (1) CI green; (2) every new `axiom` is
classified into Box 2 or Box 3 in AXIOM_AUDIT.md at the same commit; (3) no
statement moves boxes without a proof or a retraction note.
