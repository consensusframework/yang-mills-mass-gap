# Archive — redundant version variants (dedup, July 2026)

Files moved out of the module tree during Fase 1 hygiene. One canonical
version per family remains in YangMills/. Nothing deleted — history and
variants preserved here for comparison. Variants are NOT built by CI.

Canonical choices:
- EntropicPrinciple family → `EntropicPrinciple_Standalone.lean` kept
  (only variant that compiles); `EntropicPrinciple.lean` (truncated),
  OPUS45_ORIGINAL, v2, v2_fixed, v3, v4, v5(1)-duplicate archived.
- R1 family → `Gap4/RicciLowerBound/R1_RicciWellDefined.lean` kept;
  *_FOR_CLAUDE working copies archived.
- R4 family → `R4_BishopGromov.lean` kept; ROUND2 archived.
